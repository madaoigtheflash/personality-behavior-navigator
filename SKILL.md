---
name: personality-behavior-navigator
description: 用十二星座文化原型、MBTI 16 型与荣格八种认知功能辅助认识自己或他人的沟通偏好，并把观察转成可验证假设、情境适配、边界判断与行为训练。用于询问某星座/MBTI/八维的定义、识别线索和相处方法；比较两人或多人的匹配与摩擦；分析白羊 INFJ 等组合；设计冲突、协作、初识、亲密关系中的表达方式；制定八维成长训练、复盘旧模式，或检索完整的 12×16×4 组合行为库。不得用来读心、诊断、预测命运或否定明确拒绝。
---

# 人格行为导航

默认用中文。把星座、MBTI 和认知功能当作“提出假设的语言”，把持续行为、明确表达、边界和现实行动当作决策依据。

## 固定工作流

1. 识别任务：自我认识、他人识别、双方匹配、冲突修复、日常沟通或训练计划。
2. 提取事实：只记录可观察行为、原话、场景、频率、后果与双方边界；先不贴类型。
3. 检索知识库：运行 `python scripts/search_kb.py "查询" --top 8`。已知星座、MBTI 或场景时增加 `--sign`、`--mbti`、`--context` 过滤。
4. 分层解释：依次给出“事实、类型假设、替代解释、验证问题”。星座只能作为低权重文化原型。
5. 转成行动：给出一项低风险适配、一个可观察验证指标和一个停止/降级条件。
6. 更新模型：新证据不符合时修改假设，不把对方硬塞回标签。

## 输出格式

- **观察事实**：哪些是已知事实，哪些信息缺失。
- **类型假设**：涉及的星座原型、MBTI 或认知功能；标注低/中置信度。
- **替代解释**：至少一个不依赖类型的解释，例如关系阶段、压力、兴趣、能力、文化或边界。
- **识别验证**：1–3 个低压力问题或下一步观察点。
- **相处/调整动作**：最多三项，具体到措辞、时机或行为。
- **退出条件**：明确拒绝、不回应、失信或越界时如何停止、降级或寻求支持。

## 参考路由

- 每次涉及判断或匹配，先读取 `references/evidence-boundaries.md`。
- 需要掌握 12 星座、16 型和八维的定义/识别/使用时，读取 `references/taxonomy.md`。
- 需要理解 12×16×4 组合库如何生成、何时不能相加时，读取 `references/combination-method.md`。
- 需要检索命令、过滤字段、问法模板和输出解读时，读取 `references/query-guide.md`。
- 用户问“小红书都怎么说”、网络共识或流行误区时，读取 `references/xiaohongshu-consensus.md`。
- 需要追溯本项目原始文档与研究边界时，读取 `references/source-map.md`。

## 检索命令

在本 Skill 根目录运行：

```powershell
python scripts/search_kb.py "白羊座 INFJ 冲突后容易过度解释，怎么调整" --top 8
python scripts/search_kb.py "天秤座 ENFP 同事协作" --sign 天秤座 --mbti ENFP --context 协作 --top 6
python scripts/search_kb.py "Ni Fe Ti Se 如何平衡" --category function --top 8
```

若 `data/manifest.json` 不存在或源脚本更新，先运行：

```powershell
python scripts/build_kb.py
python scripts/validate_kb.py
```

## 核心边界

- 明确表达、同意、拒绝、安全、守信和现实行动的权重永远高于类型。
- 不宣称星座能预测人格，不宣称 MBTI/八维能精准识人或固定配对。
- 不根据单句、头像、生日或一次测试给他人定型；类型最多是待验证假设。
- 不把冷淡、拒绝、拉黑、疏远解释成“口是心非”“考验”或某种人格特征。
- 不为操纵、跟踪、施压、绕过边界、制造嫉妒或情感依赖提供策略。
- 涉及严重心理困扰、暴力、自伤或安全风险时，优先建议现实支持和专业帮助。
